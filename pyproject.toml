[project]
name = "extensions"
version = "0.4.0"
description = "OpenHands extensions, plugins, and skills"
requires-python = ">=3.12"

[dependency-groups]
test = [
    "pytest>=8.0",
    "requests>=2.31",
    "openhands-sdk>=0.3",
]
